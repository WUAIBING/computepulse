# 数据整合与验证机制

## 📋 概述

ComputePulse 采用**三层数据架构**：数据采集层、数据整合层、数据验证层。

```
┌─────────────────────────────────────────────────────────┐
│                    数据采集层                            │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │  Qwen    │  │ DeepSeek │  │ Doubao   │              │
│  │  (快速)  │  │  (推理)  │  │ (联网)   │              │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘              │
└───────┼─────────────┼─────────────┼────────────────────┘
        │             │             │
        ▼             ▼             ▼
┌─────────────────────────────────────────────────────────┐
│                    数据整合层                            │
│  ┌──────────────────────────────────────────────────┐  │
│  │  merge_data_improved()                           │  │
│  │  优先级: DeepSeek > Qwen > Doubao > 现有数据    │  │
│  └──────────────────────────────────────────────────┘  │
└───────────────────────┬─────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│                    数据验证层                            │
│  ┌──────────────────────────────────────────────────┐  │
│  │  Doubao Validator (数据审核员)                   │  │
│  │  - 价格合理性检查                                │  │
│  │  - 数据完整性检查                                │  │
│  │  - 市场趋势验证                                  │  │
│  │  - 交叉验证                                      │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

---

## 🔄 数据整合逻辑

### 1. 数据采集

**三个数据源**：
- **Qwen（通义千问）**：快速、稳定、联网搜索
- **DeepSeek（深度求索）**：推理模式、深度分析、数据量大
- **Doubao（豆包）**：web_search 工具、深度联网（可选，默认禁用）

### 2. 数据合并策略

**优先级规则**：
```python
DeepSeek > Qwen > Doubao > 现有数据
```

**合并逻辑**：
```python
def merge_data_improved(qwen_data, deepseek_data, doubao_data, existing_data, key_func):
    """
    1. 从现有数据开始
    2. 用 Doubao 数据覆盖（如果有）
    3. 用 Qwen 数据覆盖
    4. 用 DeepSeek 数据覆盖（最高优先级）
    """
    merged_dict = {}
    
    # 步骤 1: 加载现有数据
    for item in existing_data:
        key = key_func(item)  # 例如: "AWS_US-East_H100"
        merged_dict[key] = item
    
    # 步骤 2: 合并 Doubao 数据（如果启用）
    for item in doubao_data:
        key = key_func(item)
        if key not in merged_dict:  # 只添加新记录
            merged_dict[key] = item
    
    # 步骤 3: 合并 Qwen 数据
    for item in qwen_data:
        key = key_func(item)
        merged_dict[key] = item  # 覆盖现有记录
    
    # 步骤 4: 合并 DeepSeek 数据（最高优先级）
    for item in deepseek_data:
        key = key_func(item)
        merged_dict[key] = item  # 覆盖所有记录
    
    return list(merged_dict.values())
```

### 3. 去重机制

**Key 函数**：
- **GPU 数据**：`f"{provider}_{region}_{gpu}"`
  - 例如：`"AWS_US-East_H100"`
- **Token 数据**：`f"{provider}_{model}"`
  - 例如：`"OpenAI_GPT-4o"`

**去重规则**：
- 相同 key 的记录，后来的覆盖先前的
- 优先级高的数据源覆盖优先级低的

---

## ✅ 数据验证机制

### Doubao 作为验证层

**为什么选择 Doubao**：
1. ✅ 有 web_search 工具，可以联网验证
2. ✅ 响应慢不影响（验证是异步的）
3. ✅ 深度分析能力强
4. ✅ 不参与数据采集，避免冲突

### 验证流程

```python
def validate_with_doubao(gpu_data, token_data):
    """
    1. 价格合理性检查
    2. 数据完整性检查
    3. 市场趋势验证
    4. 生成验证报告
    """
```

### 验证内容

#### 1. 价格合理性检查

**GPU 价格**：
- ✅ 识别异常高价（与市场平均偏离 >50%）
- ✅ 识别异常低价（可能是错误数据）
- ✅ 检查同一 GPU 在不同供应商的价格差异
- ✅ 标注可疑的价格数据

**Token 价格**：
- ✅ 检查输入/输出价格比例（通常 2-4 倍）
- ✅ 对比同类模型的价格差异
- ✅ 识别异常定价

#### 2. 数据完整性检查

- ✅ 检查重复记录
- ✅ 检查必需字段是否完整
- ✅ 识别数据格式问题
- ✅ 验证数据类型

#### 3. 市场趋势验证

- ✅ 基于最新市场信息验证价格
- ✅ 识别过时的价格数据
- ✅ 提供市场洞察

#### 4. 交叉验证（可选）

- ✅ 对比不同数据源的数据
- ✅ 识别冲突和差异
- ✅ 推荐应该采用哪个数据源

### 验证报告格式

```json
{
  "summary": {
    "total_records": 77,
    "valid_records": 70,
    "suspicious_records": 5,
    "duplicate_records": 2
  },
  "anomalies": [
    {
      "provider": "阿里云",
      "gpu": "H100",
      "price": 27.78,
      "issue": "价格异常高，比市场平均高 10 倍",
      "severity": "high",
      "recommendation": "需要人工核实，可能是配置错误或包含其他服务"
    }
  ],
  "market_insights": "当前 H100 市场价格在 $2-4/小时，供应充足，价格下降趋势明显",
  "overall_quality": "good"
}
```

---

## 🎯 完整工作流程

### 步骤 1：数据采集（并行）

```python
# 同时调用三个数据源
qwen_content = call_qwen_with_search(prompt)
deepseek_content = call_deepseek_with_reasoning(prompt)
doubao_content = call_doubao_with_search(prompt) if ENABLE_DOUBAO else None
```

### 步骤 2：数据解析

```python
# 解析 JSON 响应
qwen_data = clean_and_parse_json(qwen_content)
deepseek_data = clean_and_parse_json(deepseek_content)
doubao_data = clean_and_parse_json(doubao_content)
```

### 步骤 3：数据验证（基础）

```python
# 价格范围验证
if isinstance(qwen_data, list):
    qwen_data = [item for item in qwen_data if validate_gpu_price(item)]
```

**验证规则**：
- GPU 价格：$0.1 - $50.0 /小时
- Token 价格：$0.001 - $100.0 /百万 tokens
- 电价：$0.01 - $1.0 /kWh

### 步骤 4：数据合并

```python
# 加载现有数据
existing_data = load_existing_data()

# 合并数据（按优先级）
final_data = merge_data_improved(
    qwen_data, 
    deepseek_data, 
    doubao_data, 
    existing_data, 
    key_func
)
```

### 步骤 5：保存数据

```python
# 保存到文件
with open(GPU_FILE, 'w', encoding='utf-8') as f:
    json.dump(final_data, f, indent=2, ensure_ascii=False)
```

### 步骤 6：Doubao 深度验证（异步）

```python
# 使用 Doubao 进行深度验证
validate_with_doubao(gpu_data, token_data)

# 生成验证报告
# - validation_report_gpu.json
# - validation_report_token.json
```

---

## 📊 数据质量保证

### 多层验证机制

| 层级 | 验证内容 | 工具 | 时机 |
|------|---------|------|------|
| **L1** | 数据类型、必需字段 | Python 类型检查 | 解析时 |
| **L2** | 价格范围、数值合理性 | 阈值验证 | 合并前 |
| **L3** | 去重、冲突解决 | Key 函数 | 合并时 |
| **L4** | 市场趋势、异常检测 | Doubao AI | 保存后 |

### 数据来源可追溯

每条数据都可以追溯到来源：
```python
# 日志记录
print(f"GPU Prices updated: {len(final_data)} records")
print(f"  Qwen: {len(qwen_data)}")
print(f"  DeepSeek: {len(deepseek_data)}")
print(f"  Doubao: {len(doubao_data)}")
```

---

## 🚀 使用方法

### 运行完整流程

```bash
# 数据采集 + 整合 + 验证
python scripts/fetch_prices_optimized.py --once
```

### 单独运行验证

```bash
# 只验证现有数据
python scripts/test_doubao_validator.py
```

### 查看验证报告

```bash
# GPU 验证报告
cat public/data/validation_report_gpu.json

# Token 验证报告
cat public/data/validation_report_token.json
```

---

## ⚙️ 配置选项

### 启用/禁用 Doubao 数据采集

```python
# 在 fetch_prices_optimized.py 中
ENABLE_DOUBAO = False  # 默认禁用（太慢）
```

### 调整验证超时

```python
# 在 data_validator.py 中
def call_doubao_validator(prompt, timeout=180):  # 默认 3 分钟
    ...
```

### 调整价格阈值

```python
# 在 fetch_prices_optimized.py 中
GPU_PRICE_MIN = 0.1
GPU_PRICE_MAX = 50.0
TOKEN_PRICE_MIN = 0.001
TOKEN_PRICE_MAX = 100.0
```

---

## 📈 性能优化

### 并行采集

三个数据源并行调用，不会相互阻塞：
```python
# 同时发起请求
qwen_content = call_qwen_with_search(prompt)      # ~60s
deepseek_content = call_deepseek_with_reasoning(prompt)  # ~60s
doubao_content = call_doubao_with_search(prompt)  # ~120s (可选)
```

### 异步验证

验证不阻塞主流程：
```python
# 数据已保存，验证在后台进行
save_data(final_data)
validate_with_doubao(final_data)  # 异步，不阻塞
```

### 智能缓存

保留现有数据，只更新变化的部分：
```python
# 现有数据作为基础
merged_dict = {key: item for item in existing_data}

# 只更新有变化的记录
for item in new_data:
    merged_dict[key] = item
```

---

## 🎯 最佳实践

### 1. 数据采集

- ✅ 使用 Qwen + DeepSeek 作为主力
- ✅ Doubao 仅在需要最大覆盖时启用
- ✅ 设置合理的超时时间

### 2. 数据整合

- ✅ 信任 DeepSeek 的推理结果
- ✅ 保留现有数据作为基础
- ✅ 使用明确的 key 函数去重

### 3. 数据验证

- ✅ 定期运行 Doubao 验证
- ✅ 关注验证报告中的异常
- ✅ 人工审核高严重度问题

### 4. 数据维护

- ✅ 定期清理过时数据
- ✅ 监控数据质量趋势
- ✅ 保存历史验证报告

---

## 📝 总结

### 数据整合

- **三重数据源**：Qwen + DeepSeek + Doubao（可选）
- **智能合并**：按优先级覆盖，去重
- **数据量提升**：从 35 条增加到 77 条（+120%）

### 数据验证

- **Doubao 验证层**：专门负责数据质量检查
- **多维度验证**：价格、完整性、市场趋势
- **自动化报告**：JSON 格式，易于分析

### 质量保证

- **四层验证**：类型 → 范围 → 去重 → AI 审核
- **可追溯性**：每条数据都有来源记录
- **持续改进**：基于验证报告优化数据源

---

**文档版本**：1.0  
**更新时间**：2025年12月04日  
**维护者**：ComputePulse Team
